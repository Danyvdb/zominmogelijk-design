# Zo Min Mogelijk — designpreview

Klikbare HTML-mockup van de herbouwde site (niet live).

## Lokaal bekijken

```bash
python3 -m http.server 8765
# open http://127.0.0.1:8765/
```

## Pagina’s

- `index.html` — homepage (first screen + funnel-fixes)
- `scan.html` — scan-start
- `over-ons.html` — origin story
- `maatwerk.html` — escalatie + uitzondering
- `shots/` — screenshots van de preview

Gebaseerd op analyse in sibling map `zomin-propositie` (niet in deze repo tenzij later toegevoegd).
